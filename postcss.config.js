// PostCSS is the tool that processes Tailwind's directives (@tailwind base, etc.)
// and autoprefixer adds vendor prefixes for cross-browser compatibility
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
